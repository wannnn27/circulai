PORT=4000

# Keep this key only on the backend. Never put it in the Expo app.
MIDTRANS_SERVER_KEY=SB-Mid-server-your-sandbox-key
MIDTRANS_IS_PRODUCTION=false

# Optional custom store path. Default: server/data/store.json
# CIRCULAI_STORE_FILE=server/data/store.json
